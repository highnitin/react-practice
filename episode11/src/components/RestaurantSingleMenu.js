const RestaurantSingleMenu = ({ individualMenuList }) => {
	// console.log(individualMenuList);
	return (
		<section className="restaurantSingleMenu">
			<h1>Hello from single menu</h1>
		</section>
	);
};

export default RestaurantSingleMenu;
